# --- Rust / Cargo ---
/target
**/*.rs.bk
Cargo.lock

# --- Node / JS / TS ---
node_modules
.npm
dist
lib
yarn-error.log
.eslintcache
.prettiercache

# --- Anchor / Solana ---
.anchor
.program-ids.json
**/*.so
**/*.log

# --- Deploy Keys (CRITICAL: 보안 주의) ---
# 로컬 개발용 키페어와 배포용 지갑 파일은 절대 레포지토리에 포함하지 않음
deploy/*.json
test-ledger/
/target/deploy/*.json

# --- Environment Variables ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- IDE / OS ---
.vscode
.idea
.DS_Store
thumbs.db
